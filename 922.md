# LLM Verifier 多端移植 全面复盘与交接文档

- 撰写日期：2026-09-22
- 撰写背景：主上叫停继续部署，要求把整条工作线做全面详细的复盘交接
- 交接对象：任何后续接手本项目的代理 / 工程师
- 工作区：`G:\zcode-project\llm-verify`（仅目录，**不是** git 仓库）
- 仓库根：`G:\zcode-project\llm-verify\dsh-llm-verifier`（.git 在这里，本文档也已移入此处）

---

## 0. TL;DR（三十秒版）

**已完成**：把 dsh-llm-verifier（best-of-N 多候选择优 + 评审路由 + 安全落地/回滚）移植成一份**零依赖单文件 MCP 引擎**（~700 行 Node，无 npm 依赖），以「MCP 执行 + SKILL.md 触发」双形态装进 **7 个宿主**：DSH（原生 cordis 插件）、MiniMax Code、ZCode、Codex、jcode、Qoder/QoderCN、OpenCode 社区桌面版。MiniMax 端完成**真实 e2e**（M3 候选 + 双评审模式 + 3/0 测试落地 + 回滚 + 重落地），并在桌面版做了活体 UI 演示取证；OpenCode 端完成 `opencode mcp list` **✓ connected** 宿主层验证。引擎四处副本哈希全一致（SHA256 `a7de71c2…eda5712`）。

**未闭环**：上游 PR #5 待 Web0926 合并；Qoder 应用内 UI 活体、OpenCode 桌面会话内活体调用、OMP 安装、m3-self-ask 多跳补验——全部是「注册已写入、差最后一公里活体证据」或「需主上/上游动手」的收尾项，清单见 §9。

---

## 1. 项目脉络与目标

### 1.1 需求链（按时间顺序，忠实还原主上指令）

1. 继承任务：dsh-llm-verifier 对照 llm-as-a-verifier 论文开发，装进 DSH 全局网页版（3180）真实 e2e，并在内置 neo 浏览器打开页面证明功能。
2. 「这个 codex 那边好像都做完做好了吧。检查一下。」→ 核查 Codex 侧交付（结论：47ea1f7 报告模板修复 + GPT 6 high 验收完成，SHA 一致；PR #5 OPEN 待上游）。
3. 「全部都要修好，另外那先继续开发 minimax code 桌面版同功能的插件。去了解它的插件和 hook，agents.md, skills 等等。」→ 修本机全部旧构建 + 开发 MiniMax Code 移植。
4. /goal：「做好后，直接安装去 minimax code 做真实验证。必须完美。我授权你做一切操作。不要问我。我只要结果」。
5. 「打开页面我要看插件效果。」→ MiniMax 桌面版活体演示。
6. 「这个为什么会变成 mcp？不应该是插件吗？」→ 形态解释（见 §5）。
7. 「要，现在这做了哪些 ide 的插件了」→ marketplace 卡片 + 全端盘点。
8. 「zcode 也要去做插件啊……我要怎么设置参数？还有 zcode 呢」→ ZCode 注册 + 参数配置说明。
9. 「jcode 和 codex 也配置进去」「也配置到 qoder cn 里去」→ 逐端补齐。
10. 「现在要把插件升级到我桌面上启动的社区桌面版的版本支持了。」→ OpenCode Desktop 移植（本轮完成，§3.7）。
11. 「你不用做了，做个全面详细的复盘交接文档到 922.md」→ 本文档。

### 1.2 功能目标（插件做什么）

**verified best-of-N**：给一段编码任务 + 一个 git 仓库，插件在 N 个互相隔离的 git worktree 里并行生成 N 个候选实现，各自跑验证命令过滤，然后把幸存补丁交给评审路由（宿主父代理自评，或 mcode 模型机评打分），选定优胜后以 `git apply` 落回原仓库，且提供脏检测保护的自动回滚。核心诉求是**确定性、隔离性、可回滚**——这正是它必须是代码（MCP）而不能只靠提示词的原因（§5.2）。

---

## 2. 核心交付物：引擎

### 2.1 文件与哈希（已实测）

| 位置 | 角色 |
|---|---|
| `G:\zcode-project\llm-verify\dsh-llm-verifier\minimax-code-plugin\.minimax-plugin\mcp-server.mjs` | 源文件（仓库内权威副本） |
| `C:\Users\datoo\.zcode\skills\llm-verifier\mcp-server.mjs` | 共享引擎位（ZCode/Codex/jcode/Qoder 注册指向此处） |
| `C:\Users\datoo\.config\opencode\skills\llm-verifier\mcp-server.mjs` | OpenCode 桌面版副本 |
| `C:\Users\datoo\.minimax\plugins\llm-verifier\.minimax-plugin\mcp-server.mjs` | MiniMax 安装位 |

**四处副本字节完全一致**（2026-09-22 重测）：
- SHA256 = `a7de71c2a1fed92b74f4a1225eeb23d291f628a8ef9b21d7cfa4640cceda5712`
- MD5 = `2ba140b3fdf54453e71b8588537b1d0c`（⚠️ 本节此前把这个 MD5 标成了 SHA256，用 `sha256sum` 比对会永远不一致而误判成引擎漂移）

防漂移检查法：`sha256sum` 四份，比对上面那个 64 位值。

### 2.2 技术形态

- 零依赖 Node ESM 单文件，手工实现 MCP stdio 协议（JSON-RPC 按行分帧：`initialize` → `tools/list` → `tools/call`），不依赖任何 MCP SDK——这保证了任何有 Node 的宿主即插即用。
- 头部常量：`PLUGIN_NAME="llm-verifier"`、`VERSION="1.0.0"`；`DATA_DIR` 由 `LLM_VERIFIER_DATA` 环境变量控制（各端隔离的数据根），默认 `~/.minimax/plugins/llm-verifier/data`；`MCODE_BIN` 由 `LLM_VERIFIER_MCODE_BIN` 覆盖（默认 `mcode`）。

### 2.3 暴露的 6 个工具（stdio 握手实测回执，本轮在 OpenCode 引擎副本上重跑验证）

| 工具 | 作用 |
|---|---|
| `verified_best_of` | 主流程：worktree 隔离并发生成候选 → 验证 → 冻结补丁 → 评审路由；返回 `status`（`winner_selected` / `review_pending` / `no_winner` / `failed`）、`reportPath`、各 `candidate-N.patch` |
| `select_verified_candidate` | parent_agent 模式下父模型提交 `candidateId` + 理由，进入 winner 态 |
| `apply_verified_winner` | 落地优胜补丁；前置守卫：status ∈ {winner_selected, rolled_back} + `git apply --check` |
| `rollback_verified_winner` | 反向 apply；`git apply -R --check` 脏检测，落地后被人工改过则拒绝 |
| `verifier_get_config` | 读当前配置与落盘路径（最轻的存活探针，活体演示都用它） |
| `verifier_configure` | 改配置（候选数、评审模式、验证命令、超时、模型选择等） |

### 2.4 关键配置默认值（`verifier_get_config` 实测输出）

```json
{ "enabled": true, "defaultCandidateCount": 2, "maxConcurrent": 2,
  "validationCommand": "",            // 空 → 自动探测 package.json test 脚本 → npm test
  "reviewMode": "parent_agent",       // 或 mcode_model（机器评审）
  "reviewerModel": "", "candidateModel": "",
  "candidateTimeoutMin": 20, "totalTimeoutMin": 45, "reviewTimeoutMin": 5 }
```

### 2.5 状态机与不变量（改代码前必读）

1. `runVerifiedBestOf`：`git worktree add -b <branch> <path> <base>` → 并发 worker（`runCandidate` + `runValidation` + `saveRun`）→ patch 冻结（候选 worktree 内 `git add -A` + `git diff --cached --binary` 写 `candidate-N.patch`）→ passed 过滤 → 评审路由（`mcode_model` 走 `reviewWithMcodeModel`；单幸存者直接 winner；否则 `review_pending`）。
2. 评审兜底：`--output-schema` 在 MiniMax M3/M2.7 服务端**实测不可用**（"Structured output was not valid JSON"），故 `execReview` 双跑：带 schema 失败 → 去 schema 裸 JSON 重试 → `parseSchemaJson` 宽松解析（正则 `\{[\s\S]*"scores"[\s\S]*\}`）→ selected 非最高分则纠正。
3. mcode exec 调用契约：prompt 一律走 `--input -` stdin（argv 传多行在 Windows shell:true 下被换行截断——候选全灭的元凶），`--permission off`、`--output-format json`，输出按 `"exec.result"` 行取回。
4. 候选隔离契约：候选 prompt 含 ISOLATION CONTRACT；**禁止**在运行期编辑 origin repo；每仓库同时只允许一个 run。

---

## 3. 全端部署矩阵

> 表格里的「验证层级」是诚实分级：L1 协议握手 / L2 mock e2e / L3 真实模型 e2e / L4 宿主 connected / L5 应用内活体演示。

### 3.1 DSH（DeepSeek Harness，0.1.2 Windows）
- 形态：**原生 cordis 插件**（`src/index.ts` 工具注册 + hook：`ctx.tools.guard` / `tools/result` 路由门禁 + dsh.client 设置卡片），**不是** MCP。
- 实例：3180 全局实例（Servy 服务 `dsh-web-global`，健康监控自动重启，管理员操作需 UAC）；3184 专属验收实例（`DSH_HOME=C:\Users\datoo\.dsh-phase0` + `--patch C:\Users\datoo\.dsh\phase0-approval.patch.yml`，approval never + danger-full-access 配对）。
- 修复状态：本机 6 处实例构建已统一到修复构建 **SHA 806410**。
- 验证层级：L5——neo 浏览器活体截图 5 张（`docs/proof/neo-proof-*.png`：设置卡片、高级组展开/收缩、绿色 dsh_model 评审 95 分运行）+ web RPC 全链取证。上游 **PR #5 OPEN**（Web0926/dsh-llm-verifier）。

### 3.2 MiniMax Code（mcode CLI 0.4.7 + 桌面 3.0.70.155）
- 形态：**MCP + Skill 双通道**（扩展点只有 MCP/Skill/Hook 三种，无原生工具注册面，故插件体系的落点 = `.minimax-plugin/` 清单：plugin.json（agent-plugins.org 1.0.0 schema）+ mcp.json + skills/llm-verifier/SKILL.md）。
- 注册点（全部已写入）：
  - `~/.minimax/plugins/llm-verifier/`（安装位；`~/.mavis` 是指向 `~/.minimax` 的**符号链接**，两处即同一树）
  - `~/.minimax/mcp/mcp.json` 与 `~/.minimax/mcp.json` 双文件，**`"configured": true` 是加载硬门槛**（缺它 SERVER_ABSENT，对比能加载条目实证）
  - 个人插件目录形态：根级 plugin.json（schemaVersion 1 + `mcpServers:["mcp.json"]`）
  - `mcode plugin list` 只显示 marketplace 插件，个人插件不进列表——别误判为「没装上」。
- SKILL.md 实体安装位：`~/.minimax/skills/llm-verifier/SKILL.md`。
- 验证层级：**L3 + L5（全端最高）**：
  - mock 全状态机 e2e（`tests/mock-mcode.mjs` 确定性替身）通过，含 rollback 后重 apply 场景（为此给 `apply_verified_winner` 加了 `rolled_back` 合法态，DSH parity）；
    - ⚠️ **2026-09-22 接手发现**：这条证据当时**不可复跑** —— `mock-mcode.mjs` 只是假 mcode 二进制，跑状态机的驱动脚本从没进仓库，`cd minimax-code-plugin && node --test` 结果是 `tests 0`。已补 `tests/e2e.test.mjs`（真 spawn 引擎 stdio，覆盖 6 工具 → review_pending → 未选先 apply 被拒 → select/apply/rollback/重 apply → 脏检测拒绝回滚 → mcode_model 兜底 → enabled:false 门禁），现 3/3 PASS。改引擎后请跑它。
  - 真实 e2e：M3 双候选、parent_agent 与 mcode_model 双模式、npm test 3/0 落地、回滚、再落地；证据在 `docs/proof/minimax/`（验收矩阵 README、final 报告、mcode-model 报告、两份 candidate patch、review_pending run JSON）；
  - 桌面活体：CDP 驱动真实调用 `verifier_get_config`，工具行截图（`mcp__llm_verifier_…__verifier_get_config`）；
  - 会话取证（本轮新增）：`mvs_506a0ea58add43c0b5602b1b6290bdae`（`/llm-verifier 1+1=`）完整走通 **slash 注册 → 系统提醒注入 → skill 工具加载**（`found:true, owner:"desktop", source:"global"`），模型按契约拒绝垃圾输入并索要 repoPath+task——**证明的是 Skill 触发半链**；MCP 工具执行链在该会话零调用，执行层证据仍以活体演示截图为准。落盘位置：`~/.minimax/v2/sessions/2026/09/19/16-55-52-963-session_bXZzXz…/messages.jsonl`。

### 3.3 ZCode
- 注册点：`~/.zcode/cli/config.json → mcp.servers.llm-verifier`（备份 `config.json.bak-llmverifier`），env `LLM_VERIFIER_DATA=C:/Users/datoo/.zcode/llm-verifier-data`。
- 引擎：指向 `~/.zcode/skills/llm-verifier/mcp-server.mjs`。
- 验证层级：L1 + 注册点回读；应用内活体调用未单列截图。
- ⚠️ **2026-09-22 接手时实测：该条目 `"enabled": false`** —— 注册点写得对但宿主不加载，ZCode 会话里看不到任何 llm-verifier 工具。这是七端里唯一一个「已注册却被禁用」的端，此前 §3.3 未标注。
- 注意：ZCode 用户级 MCP/Hook 只认 `~/.zcode/cli/config.json`，**不执行**工作区 `.zcode/config.json` / `zcode.json` 里的 Hook（当前版本行为，AGENTS.md 已载明）。

### 3.4 Codex
- 注册点：`codex mcp add` 写入 `~/.codex/config.toml`（备份 `config.toml.bak-llmverifier`）。
- 数据目录：独立（`43c9083` 提交说明：same engine, per-host data dirs）。
- 验证层级：L1 + 注册点回读；Codex 侧另有独立交付线（DSH 插件 47ea1f7 + GPT 6 high 3180 验收，证据 `docs/proof/gpt6-high-3180-*`）。

### 3.5 jcode
- 注册点：`~/.jcode/mcp.json → servers`（备份 `mcp.json.bak-llmverifier`）。
- 验证层级：L1 + 注册点回读。

### 3.6 Qoder / QoderCN
- 注册点：`~/.config/QoderCN/SharedClientCache/mcp.json` 与 `~/.config/Qoder/SharedClientCache/mcp.json`（两版本都写；路径是**从各 app.asar 二进制 grep 产品名实证**的，不是猜的，`f25f4e8` 提交说明）。
- 验证层级：L1；应用内活体**阻塞**：CN build 吞 `--remote-debugging-port`（12 进程但端口不监听）→ CDP 不可用，computer-use 不可用。已给主上手动 10 秒终验路径：设置 → MCP 看 llm-verifier 条目，或新会话调 `verifier_get_config`。

### 3.7 OpenCode 社区桌面版（本轮新完成）
- 宿主实证：opencode-ai **1.18.29**（npm 包 package.json 回读），桌面 `@opencode-aidesktop`；桌面版与 CLI 共用 `~/.config/opencode` 配置源。
- 原生机制（全部官方文档核实，非记忆断言）：
  - local MCP = `"type":"local"` + `command` 数组 + `environment` 对象 + `timeout`（ms，默认 5000——**必须调大**，候选 run 动辄分钟级）；
  - skill 发现约定：`~/.config/opencode/skills/<name>/SKILL.md`，frontmatter 必填 `name`（与目录同名）+ `description`。
- 动作：引擎 + SKILL.md 复制到 `~/.config/opencode/skills/llm-verifier/`；`llm-verifier` 条目写入 **`opencode.jsonc` 与 `opencode.json` 双文件**（两文件并存且优先级官方未载明 → 双写消歧义，均先验证为严格合法 JSON 再补丁，`enabled:true`、`environment.LLM_VERIFIER_DATA=~/.config/opencode/llm-verifier-data`、`timeout:60000`）。
- 验证层级：**L1 + L4**：
  - stdio 直连握手：INIT 回 `{name:"llm-verifier",version:"1.0.0"}`、TOOLS 列全 6 工具、CONFIG 显示数据目录隔离生效；
  - `opencode mcp list`（rc=0，14 服务器）：**`✓ llm-verifier connected  node C:/Users/datoo/.config/opencode/skills/llm-verifier/mcp-server.mjs`**。对照组：browseros-neo/web-search-prime/zread 显示 failed（9010 未监听等环境问题），证明该命令如实反映连接状态，✓ 不是摆设。
- 未做（诚实声明）：桌面 App 当时未运行，应用内会话里 `verifier_get_config` 活体调用未做。

### 3.8 OMP（状态：未装）
- 主上过问「那 omp 呢」，未列入后续指令；且本机 omp 仓库处于另一条 bazel 工作线中间态（CLI shim 报错，`bun run cli.ts` 直跑可通）。**未做，属遗留**（§9）。

---

## 4. 姊妹插件 m3-self-ask 的形态对照

同一作者工作线里的第二件交付，用来回答主上「为什么有的要 MCP 有的只要 skill」：

- MiniMax Code：**纯 SKILL.md**（Self-Ask 协议每步都是对话内文本推理，无子进程、无文件系统副作用、无隔离/原子性要求，skill 即完整实现）。
- DSH：升级为**原生 cordis plugin**（带 `tools.guard` hook、路由门禁）——因为 DSH 侧要求 enforcement（强制执行协议）而非模型自觉；一旦需要强制，就必须回到代码阵营。
- trivial 冒烟已过：`/m3-self-ask 1+1=` → `final answer: 2` + JSON 回执 `{"status":"completed","steps":[],"internal_calls":0}`。
- **已知风险**：机器上存在两份内容略异的 SKILL.md（`~/.minimax/skills/m3-self-ask/` 2.2K vs `G:/CodexData/codex-home/skills/m3-self-ask/` 2.0K），MiniMax 实测加载的是 CodexData 那份——改文件前先确认加载源。
- 多跳完整行为 headless 补验未做（此前受配额窗口限制，现已过期可重试，§9）。

---

## 5. 设计决策与理由

### 5.1 为什么各端形态不一样
分界线只有一条：**功能需不需要一个真正伸进操作系统干活的代码进程**。
- 外部副作用 + 确定性/原子性要求（子进程、git worktree 隔离、apply 守卫、状态落盘）→ 必须 MCP（手脚）+ SKILL.md（说明书，教模型何时调、review_pending 怎么处理）；
- 纯推理模式 → 纯 skill 即够，加 MCP 是零收益负担。
MiniMax Code 的「插件」在宿主眼里就是 MCP+Skill 注册组合（它没有更重的原生工具面）；DSH 有真插件体系，所以那边是 cordis plugin——**同一功能，不同宿主用各自原生机制**。

### 5.2 确定性细节不可交给模型自觉
apply 前置 `--check`、rollback 脏检测、schema 失败兜底、`selected` 必须为最高分——这些如果写在提示词里，上下文一压缩就丢。全部沉进引擎代码。

### 5.3 parent_agent 评审设计（DSH 线，记忆 `parent_agent_review design`）
懒Credential + 自动选第一个 + candidateId 覆盖，彻底消除 DeepSeek API key 依赖——评审可以完全由宿主父代理完成。

### 5.4 数据隔离
每端 env `LLM_VERIFIER_DATA` 指向独立目录；引擎文件可多端共用（ZCode/Codex/jcode/Qoder 共一份），但 **run 记录、worktree、config 永不互串**。

---

## 6. 踩坑实录（现象 → 根因 → 修复 → 教训）

1. **候选全灭**：mcode exec 多行 prompt 经 Windows `shell:true` argv 截断 → 改 `--input -` stdin（先在真 mcode 验证）。教训：Windows 上传 prompt 永远走 stdin。
2. **MCP 服务器不加载（SERVER_ABSENT）**：MiniMax 条目缺 `"configured": true` → 补上即活。教训：注册门槛要对比同宿主可加载条目实证。
3. **机器评审回执空 scores**：`--output-schema` 对 minimax-legacy(M3) 与原生 minimax(M2.7) 均返回 "Structured output was not valid JSON" → 实现「带 schema → 失败裸 JSON 重试 → 宽松正则解析」双跑兜底（`81f6b24`）。教训：服务端能力以实测为准。
4. **回滚后无法重落地**：apply 守卫拒绝 `rolled_back` 态 → 加合法态（DSH parity）。教训：状态机守卫要枚举完整。
5. **`node --test test/` 在 Node 24 MODULE_NOT_FOUND**：目录被当文件 → fixture 改无参 `node --test`。
6. **rtk hook 劫持 shell**：git/curl 被包装成 exit 127 "rtk: command not found" → `command git` 绕过或换 python。当前 rtk 又报 "No hook installed" 警告（环境在变，遇到再实证）。
7. **COMET-HOOK 拦截 Write**：本工作区写 `.mjs` 脚本被 "Classic artifact layout is unavailable" 拦 → 注册/验证逻辑改 Bash+python 内联执行。本文档为 .md 直写成功。
8. **git worktree add 参数顺序**：`add wt -b branch` 失败 → `add -b branch wt base`。
9. **Qoder 调试口被吞**：CN build 带 `--remote-debugging-port` 起 12 进程但端口不监听 → 放弃 CDP，诚实改手动终验。教训：自动化不可用时给主上的替代路径比硬撑有价值。
10. **openviking noop 空转**（DSH 线 RCA）：必需工具通道死亡后曾空转 466 次请求——铁律：通道死最多重试 2 次即报告阻塞，绝不生成形式变化的伪调用硬撑。
11. **Neo/CDP 杂症**（DSH 线）：最小化窗口截图 0 width → `Emulation.setDeviceMetricsOverride`；WS 403 → `suppress_origin=True`；3180 跟 302 后 401 → 必须 cookiejar。全部记入记忆库。

---

## 7. Git 与发布状态（本轮实测回读）

- 仓库：`G:\zcode-project\llm-verify\dsh-llm-verifier`（注意：上一级 `llm-verify` 只是工作目录，无 `.git`）；分支 main；`origin/main` **ahead 28 / behind 0**。
- 远端：`origin` = https://github.com/Web0926/dsh-llm-verifier.git；`fork` = https://github.com/tcflying/dsh-llm-verifier.git（**提交链已全部推 fork**）。
- HEAD 提交链（新→旧）：
  - `f25f4e8` docs(minimax): qoder cn registration — SharedClientCache/mcp.json (asar-verified path)
  - `43c9083` docs(minimax): codex + jcode registrations — same engine, per-host data dirs
  - `cb1ba34` docs(minimax): ZCode port notes
  - `ec94509` docs(minimax): second-round verification notes — skill placement + structured-output reality
  - `81f6b24` fix(minimax): review schema fallback — bare-JSON retry + lenient parse
  - `6ccac45` feat(minimax): port llm-verifier to MiniMax Code as MCP+Skill plugin
  - `47ea1f7` fix: report actual reviewer and verify GPT 6 high on 3180
  - `8f4e514` / `44336d8` / `1df70b9` / `5a913d0` / `2a7bbfe` …（DSH 验收与 perf 链）
- 未跟踪文件：已清零。`922.md` 原本落在仓库外（任何 clone 都取不到）→ 已移入仓库纳管；`minimax-code-plugin/.claude-plugin/plugin.json` 已删除——全仓库无引用、`${PLUGIN_ROOT}` 不是 Claude Code 的变量名（应为 `${CLAUDE_PLUGIN_ROOT}`）、且 `args` 指向插件目录之外的 `../.minimax-plugin/`，按目录安装必断链。
- 上游：Web0926/dsh-llm-verifier **PR #5 OPEN 且 mergeable=MERGEABLE**（2026-09-22 `gh pr view` 实测，无冲突待解），只有上游维护者能 Merge。

---

## 8. 已知边界与风险

- 评审机评依赖 `mcode` CLI 在 PATH（`LLM_VERIFIER_MCODE_BIN` 可覆盖）；DSH 端不依赖它。
- **引擎验证步是 Windows-only**：`runValidation` 硬编码 `cmd /c <命令>`，在 Linux/macOS 上每个候选都会被判 validation failed → `no_winner`。§2.2「任何有 Node 的宿主即插即用」只对生成/评审成立，验证不成立。七端目前全在本机 Windows，未修（要修就把 `cmd /c` 换成 `process.platform==="win32"?"cmd":"/bin/sh"` 分支 + 一处引号规则）。
- 引擎 spawn `mcode` 用 `shell:true` + 参数数组，Node 24 已对此发 `DEP0190` 弃用告警（当前无实际注入面：参数全由引擎拼接）。日后若上游要求关闭，改成自己拼命令串或去掉 shell。
- 引擎对 OpenCode 只验证到连接层（L4）；真实候选 run 依赖宿主的模型配额（本轮 MiniMax 真实 e2e 用的是 `custom_provider:minimax-legacy/MiniMax-M3`；弱模型 `omniroute auto/coding:free` 会思维死循环，别拿它当候选模型）。
- `git worktree` 残留：候选 worktree 在数据目录 `worktrees/` 下持久保留（run 取证需要），磁盘配额紧的宿主（DSH 有盘量守卫先例：C 盘满曾拒 5 候选 run）注意清理。
- MiniMax 个人插件不进 `mcode plugin list`——盘点时勿据此判丢失。
- m3-self-ask 双份 SKILL.md 漂移风险（§4）。
- Qoder 活体、OpenCode 应用内活体、OMP：均「注册/机制已明、差活体」，状态见 §9。

---

## 9. 待办清单（按优先级，标注谁能解除）

| # | 事项 | 阻塞者 | 建议动作 |
|---|---|---|---|
| 1 | 上游 PR #5 合并 | Web0926（GitHub 点 Merge） | 主上催办；合并后本机实例切官方源 |
| 2 | Qoder/QoderCN 应用内活体终验 | 主上（10 秒手动） | 设置→MCP 看条目，或新会话调 `verifier_get_config` |
| 3 | OpenCode 桌面 App 内活体调用 | 主上启动桌面 App 后代理可做 | App 起来后任一会话调 `verifier_get_config` 截图 |
| 4 | ~~未跟踪 `.claude-plugin/plugin.json` 收编或删~~ **已删**；~~ZCode 被禁用~~ **已改 `enabled:true`**（备份 `config.json.bak-enable-llmverifier`），**需重启 ZCode session 才生效**，重启后 `/mcp` 应见 6 工具 | 代理已做，剩主上重启确认 | 重启后调 `verifier_get_config` 验证 |
| 5 | OMP 端安装 | omp 仓库 bazel 工作线中间态 | 那条线收敛后再装（`bun run cli.ts` 可直跑） |
| 6 | m3-self-ask 多跳 headless 补验 | 配额窗口（2026-09-20 18:06 UTC 已过） | 选一个真多跳问题跑 mcode exec 验 Follow-up 循环 |
| 7 | 922.md 之外，考虑把各端注册点写进 README「全端矩阵」总表 | 代理可做 | 本次未动 README，留作后续 |

---

## 10. 复现/自验速查

```bash
# 引擎防漂移（四份应同为 a7de71c2… 开头的 SHA256）
sha256sum "G:/zcode-project/llm-verify/dsh-llm-verifier/minimax-code-plugin/.minimax-plugin/mcp-server.mjs" \
  "C:/Users/datoo/.zcode/skills/llm-verifier/mcp-server.mjs" \
  "C:/Users/datoo/.config/opencode/skills/llm-verifier/mcp-server.mjs" \
  "C:/Users/datoo/.minimax/plugins/llm-verifier/.minimax-plugin/mcp-server.mjs"

# OpenCode 宿主连接验证
timeout 45 opencode mcp list        # 期望：✓ llm-verifier connected

# MCP stdio 握手（python 直探，绕 shell 坑）
# spawn: node <engine>；逐行喂 initialize / tools/list / tools/call(verifier_get_config)
# 期望：INIT 1.0.0 / 6 工具 / config 含 LLM_VERIFIER_DATA 路径

# MiniMax 端会话取证（桌面版 v2 落盘）
# ~/.minimax/v2/sessions/<日期>/<时间>-session_<base64sid>/messages.jsonl

# mock 全状态机 e2e（2026-09-22 补回，3/3 PASS，跑完自动清临时目录）
cd G:/zcode-project/llm-verify/dsh-llm-verifier/minimax-code-plugin && node --test

# rtk 劫持 git 时
type  # 或 command git ...
```

DSH 线复现/固定页约定、Neo/BrowserClaw CDP 运维、web RPC 信封、approval-never 策略等——已在记忆库（§附录 B），不在本文档重复。

---

## 附录 A：关键文件路径索引

| 内容 | 路径 |
|---|---|
| 引擎源 | `minimax-code-plugin/.minimax-plugin/mcp-server.mjs`（仓库根 `G:\zcode-project\llm-verify\dsh-llm-verifier`） |
| 插件清单 | `minimax-code-plugin/.minimax-plugin/{plugin.json,mcp.json,skills/llm-verifier/SKILL.md}` |
| MiniMax 安装位 | `~/.minimax/plugins/llm-verifier/`；mcp 注册 `~/.minimax/mcp/mcp.json` + `~/.minimax/mcp.json`；skill `~/.minimax/skills/llm-verifier/SKILL.md` |
| OpenCode 注册 | `~/.config/opencode/opencode.jsonc`（32-43 行）+ `opencode.json`；引擎/skill 在 `~/.config/opencode/skills/llm-verifier/`；数据 `~/.config/opencode/llm-verifier-data/` |
| ZCode 注册 | `~/.zcode/cli/config.json → mcp.servers.llm-verifier`；数据 `~/.zcode/llm-verifier-data/` |
| Codex 注册 | `~/.codex/config.toml`（codex mcp add 写入；`.bak-llmverifier` 备份在侧） |
| jcode 注册 | `~/.jcode/mcp.json → servers`（`.bak-llmverifier` 备份在侧） |
| Qoder 注册 | `~/.config/{Qoder,QoderCN}/SharedClientCache/mcp.json` |
| MiniMax 验收证据 | `docs/proof/minimax/`（README 矩阵、final/mcode-model 报告、2 patch、run JSON） |
| DSH 验收证据 | `docs/proof/neo-proof-*.png` + `gpt6-high-3180-*` |
| m3-self-ask | `~/.minimax/skills/m3-self-ask/SKILL.md`（注意与 `G:/CodexData/codex-home/skills/` 那份漂移） |
| 测试替身 | `minimax-code-plugin/tests/mock-mcode.mjs` |

## 附录 B：记忆库指针（`C:\Users\datoo\.zcode\cli\memories\projects\llm-verify-…\memory\MEMORY.md`）

- `minimax-verifier-plugin` — MiniMax 移植全记录（configured:true、stdin 坑、M3 验收）
- `dsh-env-setup` / `dsh-plugin-settings-integration` / `dsh-approval-never-policy` / `dsh-agents-eperm-workaround` / `llm-verifier-fixed-neo-page` / `browserclaw-cdp-ops` / `dsh-web-rpc-protocol` — DSH 线运维全集
- `minimax-m3-logprobs-gap` — M3 无 logprobs，不可作 verifier 后端
- `browser-automation-unreliable` — 内置浏览器自动化不可靠的教训
- `parent_agent_review design` — 评审路由设计

（本文档即最终交接件；接手者按 §9 待办顺序推进即可，所有关键事实都附了可复跑证据路径。）
