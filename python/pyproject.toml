[project]
name = "dsh-llm-verifier-bridge"
version = "0.1.0"
description = "Pinned Python bridge for dsh-llm-verifier"
requires-python = ">=3.9,<3.14"
dependencies = [
  "llm-verifier==0.2.0",
]

[tool.uv]
package = false
